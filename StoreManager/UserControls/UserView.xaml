<UserControl x:Class="StoreManager.UserControls.UserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Row="0" Grid.Column="0" Background="#1E293B" CornerRadius="20" Padding="20" Margin="0,0,15,15">
            <StackPanel>
                <TextBlock Text="👤 User Management" FontSize="26" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="Manage TechHub employees and administrators" Margin="0,2,0,15" Foreground="#94A3B8"/>

                <UniformGrid Columns="2">
                    <StackPanel Margin="5">
                        <TextBlock Text="Username" Foreground="#CBD5E1"/>
                        <TextBox x:Name="tbUsername"/>
                    </StackPanel>
                    <StackPanel Margin="5">
                        <TextBlock Text="Email" Foreground="#CBD5E1"/>
                        <TextBox x:Name="tbEmail"/>
                    </StackPanel>
                    <StackPanel Margin="5">
                        <TextBlock Text="First Name" Foreground="#CBD5E1"/>
                        <TextBox x:Name="tbFirstName"/>
                    </StackPanel>
                    <StackPanel Margin="5">
                        <TextBlock Text="Last Name" Foreground="#CBD5E1"/>
                        <TextBox x:Name="tbLastName"/>
                    </StackPanel>
                    <StackPanel Margin="5">
                        <TextBlock Text="Password" Foreground="#CBD5E1"/>
                        <PasswordBox x:Name="pbPassword"/>
                    </StackPanel>
                    <StackPanel Margin="5">
                        <TextBlock Text="Role" Foreground="#CBD5E1"/>
                        <ComboBox x:Name="cbRole"/>
                    </StackPanel>
                </UniformGrid>
            </StackPanel>
        </Border>

        <Grid Grid.Row="0" Grid.Column="1" Margin="0,0,0,15">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#38BDF8" CornerRadius="20" Padding="20" Margin="0,0,0,10">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="TECHHUB" FontSize="22" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                    <TextBlock Text="User Administration" Foreground="White" FontSize="14" Margin="0,5,0,15" HorizontalAlignment="Center"/>
                    <TextBlock Text="✔ Manage users" Foreground="White" FontSize="13" Margin="0,3"/>
                    <TextBlock Text="✔ Assign roles" Foreground="White" FontSize="13" Margin="0,3"/>
                    <TextBlock Text="✔ Secure password storage" Foreground="White" FontSize="13" Margin="0,3"/>
                </StackPanel>
            </Border>

            <StackPanel Grid.Row="1" Orientation="Vertical">
                <Button x:Name="btnAdd" Content="➕ Add User" Style="{StaticResource ActionButtonStyle}" Height="35" Margin="0,2" Click="btnAdd_Click"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="btnUpdate" Grid.Column="0" Content="✏ Update" Style="{StaticResource ActionButtonStyle}" Height="35" Margin="0,2,2,2" Click="btnUpdate_Click"/>
                    <Button x:Name="btnDelete" Grid.Column="1" Content="🗑 Delete" Style="{StaticResource ActionButtonStyle}" Height="35" Margin="2,2,0,2" Click="btnDelete_Click"/>
                </Grid>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Grid.ColumnSpan="2" Background="#1E293B" CornerRadius="20" Padding="15">
            <DataGrid x:Name="dgUsers" AutoGenerateColumns="False" SelectionChanged="dgUsers_SelectionChanged"
                      Background="#0F172A" Foreground="White" RowBackground="#1E293B" AlternatingRowBackground="#273449"
                      GridLinesVisibility="None" BorderThickness="0" HeadersVisibility="Column" CanUserAddRows="False"
                      VerticalAlignment="Stretch" VerticalContentAlignment="Stretch">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#38BDF8"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="10,8"/>
                        <Setter Property="BorderThickness" Value="0"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="1.5*"/>
                    <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="*"/>
                    <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="*"/>
                    <DataGridTextColumn Header="Role" Binding="{Binding RoleName}" Width="110"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>